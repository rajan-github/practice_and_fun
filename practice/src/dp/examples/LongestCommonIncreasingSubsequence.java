package dp.examples;

public class LongestCommonIncreasingSubsequence {
	
	/**
	 * TODO: Complete it.
	 * @param array1
	 * @param array2
	 * @return
	 */
	public static int longestCommonIncreasingSubsequence(int[] array1, int[] array2) {
		if (array1 == null || array2 == null || array1.length == 0 || array2.length == 0)
			return 0;

//		int[][] dp1 = new int[array1.length + 1][array1.length + 1];
//		int[][] dp2 = new int[array2.length + 1][array2.length + 1];

		return 0;

	}
}
